<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dossis.curso3semana4">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Curso3Semana4">
        <activity
            android:name=".Activitys.ContactoActivity"
            android:label="@string/title_activity_contacto"
            android:parentActivityName=".Activitys.MainActivity"
            android:theme="@style/Theme.Curso3Semana4.NoActionBar">

            <!-- Soporte para versiones 4.0 o menores -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.dossis.curso3semana4.Activitys.MainActivity" />
        </activity>
        <activity
            android:name=".Activitys.AcercaDeActivity"
            android:parentActivityName=".Activitys.MainActivity">

            <!-- Soporte para versiones 4.0 o menores -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.dossis.curso3semana4.Activitys.MainActivity" />
        </activity>
        <activity
            android:name=".Activitys.FavoritosActivity"
            android:parentActivityName=".Activitys.MainActivity">

            <!-- Soporte para versiones 4.0 o menores -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.dossis.curso3semana4.Activitys.MainActivity" />
        </activity>
        <activity android:name=".Activitys.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>